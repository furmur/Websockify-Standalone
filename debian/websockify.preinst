#!/bin/bash
set -e

adduser --quiet --group --disabled-password --shell /bin/false \
--gecos "yeti user interface" --no-create-home websockify || true

if [ ! -d /var/log/websockify ]; then 
    mkdir -p /var/log/websockify
fi


#DEBHELPER#

exit 0
